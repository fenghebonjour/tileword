// ─── GLOBAL SETTINGS (defaults) ──────────────────────────────────────────────
export const DEFAULT_SETTINGS = {
  smokeEffect: false,
  cheerEffect: true,
  hintPenalty: true,
  tileAnimations: true,
  soundLabels: true,
  autoMakeWord: true,
  soundEffects: 0.5,
  ambientMusic: 0.5,
  tileTheme: "neon",   // "neon" | "mahjong" | "paper"
};

